<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>
    <body>
        <%- include("../partials/header.ejs") %>
        <main>
            <div>
                <form action = "/animals/<%= animal._id %>?_method=PUT" method="post">
                    <fieldset>
                        <legend>Edit a <%= animal.species %></legend>
                        <label>
                            Species: <input type="text" name="name" value="<%= animal.species %>" placeholder="enter animal species">
                        </label>
                        <label>
                            Extinct? <input type="checkbox" name = "extinct"
                            <%= animal.extinct ? "checked" : "" %>>
                        </label>
                        <label>
                            Location: <input type="text" name="location" value="<%= animal.location %>" placeholder = "enter animal location">
                        </label>
                        <label>
                            Life Expectancy: <input type="number" name="lifeExpectancy" value="<%= animal.lifeExpectancy %>">
                        </label>
                    </fieldset>
                    <input type="submit" value="Update <%= animal.species %>">
                </form>
            </div>
        </main>
    </body>
</html>